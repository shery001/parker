#include <iostream>

long compute_scores()
{
  long scores = 0;

  // Fill this function.

  return scores;
}

int main()
{
  auto scores = compute_scores();
  std::cout << "The total sum of all name scores is " << scores << std::endl;
}